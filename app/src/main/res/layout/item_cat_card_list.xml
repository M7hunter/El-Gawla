<?xml version="1.0" encoding="utf-8"?><!-- icon container -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_margin="@dimen/margin_medium"
    android:background="@drawable/bg_circle_grey"
    android:foreground="?attr/selectableItemBackgroundBorderless">
    <!-- card icon -->
    <View
        android:id="@+id/v_card_icon_cardList"
        android:layout_width="@dimen/cat_item_card_width"
        android:layout_height="@dimen/cat_item_card_height"
        android:layout_gravity="center"
        android:layout_marginStart="@dimen/margin_2xlarge"
        android:layout_marginTop="@dimen/margin_large"
        android:layout_marginEnd="@dimen/margin_2xlarge"
        android:layout_marginBottom="@dimen/margin_large"
        android:background="@drawable/dynamic_card" />
</FrameLayout>